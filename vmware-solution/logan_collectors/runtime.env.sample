# Site-specific settings for bin/run.sh.
#
# Copy this file to runtime.env in the same directory, then adjust the paths.
# It is sourced as a Bash file, so quote values that contain spaces.

#BASE_DIR=/opt/oracle/logan_collectors

#PYTHON_BIN=/usr/bin/python3
